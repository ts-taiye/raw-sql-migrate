language: python
python:
    - 2.6
    - 2.7
addons:
  postgresql: "9.3"
install:
    - if [[ $TRAVIS_PYTHON_VERSION == '2.6' ]]; then travis_retry pip install importlib; fi
    - pip install -r requirements.txt
before_script:
  - psql -c 'create database rsm_test;' -U postgres
script:
    nosetests
